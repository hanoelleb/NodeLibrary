<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='../public/stylesheets/style.css' />
    <meta charset='UTF-8'></meta>
  </head>
  <body>
    <%- include('navbar') %>
    <h1><%= title %> : <%= genre.name %></h1>
    <% if (locals.genre_books) { %>
        <% if (genre_books.length > 0) { %>
	<p>Delete the following books before attempting to delete this genre
	</p>
	<h4>Books</h4>
        <dl>
        <% for (var i = 0; i < genre_books.length; i++) { %>
            <dt>
                <a href= <%= genre_books[i].url %> >
                    <%= genre_books[i].title %>
                </a>
            </dt>
            <dd><%= genre_books[i].summary %></dd>
        <% } %>
        </dl>
        <% } else { %>
	 <p>Do you really want to delete this Genre?</p>
        <form method='POST' action=''>
            <input type='hidden' name='genreid' required='true'
                    value=<%= genre._id %> ></input>
            <input type='submit' value='Delete'></input>
	</form>
	<% } %>
    <% } %>
  </body>
</html>

