<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='../public/stylesheets/style.css' />
    <meta charset='UTF-8'></meta>
  </head>
  <body>
    <%- include('navbar') %>
    <h1>Author: <%= author.name %></h1>
    <p>
        <%= author.formatted_birth_date %> - <%= author.formatted_death_date %>
    </p>
    <h4>Books</h4>
    <dl>
        <% for (var i = 0; i < author_books.length; i++ ) { %>
	    <dt>
	        <a href=<%= author_books[i].url %>>
                    <%= author_books[i].title %>
                </a>
	    </dt>
	    <dd>
	        <%= author_books[i].summary %>
	    </dd>
	<% } %>

	<% if (author_books.length == 0) { %>
	    <p>This author has no books.</p>
	<% } %>
    </dl>
    <hr>
        <p>
	   <a href=<%= author.url + '/delete' %>>Delete author</a>
	</p>
	<p>
           <a href=<%= author.url + '/update' %>>Update author</a>
        </p>
    </hr>
  </body>
</html>
