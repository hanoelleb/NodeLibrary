<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='../public/stylesheets/style.css' />
    <meta charset='UTF-8'></meta>
  </head>
  <body>
    <%- include('navbar') %>
    <h1>Title: <%= title %></h1>
    <p>
        <a href= <%= book.author.url%> >
	    <%= book.author.name %>
        </a>
    </p>
    <p>
        Summary: <%= book.summary %>
    </p>
    <p>
        ISBN: <%= book.isbn %>
    </p>
        Genre:
        <% for (var i = 0; i < book.genre.length; i++) { %>
	<a href= <%= book.genre[i].url %> >
	    <%= book.genre[i].name %>
	    <% if (i < book.genre.length - 1) { %>
	      ,
	    <% } %>
	</a>
	<% } %>
    <h4>Copies</h4>
	<% for (var i = 0; i < book_instances.length; i++ ) { %>
	    <hr>
	    <p>Imprint: <%=book_instances[i].imprint %></p>
	    <p>
	        ID: <a href=<%=book_instances[i].url %>><%= book_instances[i]._id %></a>
	        <span><%= book_instances[i].status %></span>
	    </p>
	    <% if (book_instances[i].status != 'Available') { %>
	        <p>Due back: <%= book_instances[i].due_back_formatted %></p>
	    <% } %>
	<% } %>


	<% if (book.genre.length == 0) { %>
	    <p>There are no copies of this book in the library.</p>
	<% } %>
  </body>
</html>

