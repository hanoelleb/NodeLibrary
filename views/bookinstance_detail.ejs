<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='../public/stylesheets/style.css' />
    <meta charset='UTF-8'></meta>
  </head>
  <body>
    <%- include('navbar') %>
    <h1>ID: <%= bookinstance._id %></h1>
    <p>Title: 
    <a href=<%= bookinstance.book.url %>>
            <%= bookinstance.book.title %>
        </a>
    </p>
    <p>Imprint: <%= bookinstance.imprint %></p>
    <p>Status: <%= bookinstance.status %></p>

    <% if ( bookinstance.status != 'Available' ) { %>
        <p>Due back: <%= bookinstance.due_back_formatted %></p>
    <% } %>

    <hr>
        <p>
           <a href=<%= bookinstance.url + '/delete' %>>Delete instance</a>
        </p>
    </hr>
  </body>
</html>
